---
title: Introduction
description: Start with Cerebrium when latency, burst traffic, and production AI constraints matter from day one.
---

Cerebrium is the inference platform for **real-time and production AI workloads**.

It is a particularly strong fit when you already know your app will care about one or more of the following:

- **Low latency and low cold starts**
- **Bursty traffic** that should scale without wasting GPU capacity
- **Multi-region deployments** for global users or data residency
- **Realtime voice, video, and streaming workloads**
- **GPU-heavy production inference** that has to stay reliable under load

If you are building a quick prototype with very little infrastructure pressure, many platforms can work. Cerebrium is designed for teams that want to start closer to their production reality instead of replatforming later.

## Why teams choose Cerebrium

- Launch code in the cloud in seconds
- Run [CPUs](/cerebrium/hardware/cpu-and-memory) or [GPUs](/cerebrium/hardware/using-gpus) with automatic scaling
- Serve [REST APIs](/cerebrium/endpoints/inference-api), [streaming endpoints](/cerebrium/endpoints/streaming), [WebSockets](/cerebrium/endpoints/websockets), or any [ASGI-compatible app](/cerebrium/container-images/custom-web-servers)
- Deploy across [multiple regions](/cerebrium/deployments/multi-region-deployment) for lower latency and residency requirements
- Tune [concurrency and batching](/cerebrium/scaling/batching-concurrency) for real production traffic
- Improve startup performance with [cold-start optimization strategies](/cerebrium/other-topics/faster-cold-starts)
- Store model weights and files with [persistent storage](/cerebrium/storage/managing-files)
- Pay only for the compute you use — [billed by the second](https://www.cerebrium.ai/pricing)

## Start by workload

If you already know what you are building, start with the closest path below:

- **OpenAI-compatible LLM endpoint** → [Serve an OpenAI Compatible LLM with vLLM](/v4/examples/gpt-oss)
- **Voice AI / real-time speech** → [Deploy a Twilio Voice Agent with Pipecat](/v4/examples/twilio-voice-agent)
- **Realtime voice agents** → [Realtime Voice Agents](/v4/examples/realtime-voice-agents)
- **Image and video generation** → [Deploy ComfyUI on Cerebrium](/v4/examples/comfyUI)
- **Migration from another platform** → [Migrate from Replicate](/migrations/replicate)

If you just want the fastest possible first deployment, follow the quickstart below.

## Quickstart

Setting up and deploying an app on Cerebrium takes just a few steps.

### 1. Install the CLI

<Tabs>
<Tab title="Python (pip)">

```bash
pip install cerebrium
```

</Tab>
<Tab title="macOS (Homebrew)">

```bash
brew tap cerebriumai/tap
brew install cerebrium
```

</Tab>
<Tab title="Linux">

```bash
# Ubuntu/Debian
wget https://github.com/CerebriumAI/cerebrium/releases/latest/download/cerebrium_linux_amd64.deb
sudo dpkg -i cerebrium_linux_amd64.deb

# Or binary installation
curl -L https://github.com/CerebriumAI/cerebrium/releases/latest/download/cerebrium_cli_linux_amd64.tar.gz | tar xz
sudo mv cerebrium /usr/local/bin/
```

</Tab>
<Tab title="Windows">

```powershell
# PowerShell (Run as Administrator)
Invoke-WebRequest -Uri "https://github.com/CerebriumAI/cerebrium/releases/latest/download/cerebrium_cli_windows_amd64.zip" -OutFile "cerebrium.zip"
Expand-Archive -Path "cerebrium.zip" -DestinationPath "."
# Add cerebrium.exe to PATH
```

</Tab>
</Tabs>

### 2. Log in to the CLI

```bash
cerebrium login
```

This opens your browser so you can authenticate your CLI session.

### 3. Initialize a project

```bash
cerebrium init my-first-app
cd my-first-app
```

This creates a basic project with `main.py` for app code and `cerebrium.toml` for configuration.

```python
def run(prompt: str):
    print(f"Running on Cerebrium: {prompt}")
    return {"my_result": prompt}
```

### 4. Run code remotely

Run the function in the cloud and pass it a prompt:

```bash
cerebrium run main.py::run --prompt "Hello World!"
```

You should see the prompt in the logs. This is useful for quick code iteration, testing snippets, or once-off scripts that need cloud CPU/GPU resources.

### 5. Deploy your app

```bash
cerebrium deploy
```

This turns the function into a persistent [REST endpoint](/cerebrium/endpoints/inference-api) that accepts JSON input and can scale automatically.

Once deployed, your app will be callable through a POST endpoint shaped like:

```text
https://api.aws.us-east-1.cerebrium.ai/v4/{project-id}/{app-name}/{function-name}
```

### 6. What to do next

After your first deployment, the most useful next steps are usually:

- [Reduce cold starts](/cerebrium/other-topics/faster-cold-starts)
- [Tune scaling and concurrency](/cerebrium/scaling/batching-concurrency)
- [Deploy to multiple regions](/cerebrium/deployments/multi-region-deployment)
- [Use OpenAI-compatible endpoints](/cerebrium/endpoints/openai-compatible-endpoints)
- [Store model weights in persistent storage](/cerebrium/storage/managing-files)

## How Cerebrium works

Cerebrium uses containerization to ensure consistent environments and reliable scaling for apps. When code is deployed, Cerebrium packages it with all necessary dependencies into a container image. This image serves as a blueprint for creating instances that handle incoming requests. The system automatically manages scaling, creating new instances when traffic increases and removing them during quiet periods.

For a detailed explanation of how Cerebrium builds and manages container images, see our [Defining Container Images Guide](/cerebrium/container-images/defining-container-images).

<Info>
  Content-Aware Storage forms the foundation of Cerebrium's speed. This system
  intelligently manages container images by understanding their content
  structure. When launching new instances, it pulls only the specific files.
  This targeted approach significantly reduces cold start times and optimizes
  resource usage.
</Info>

## Further examples

- [Serve an OpenAI Compatible LLM with vLLM](/v4/examples/gpt-oss)
- [Deploy a Twilio Voice Agent with Pipecat](/v4/examples/twilio-voice-agent)
- [Run a Hyper-Parameter Sweep Training Llama 3.2](/v4/examples/wandb-sweep)
- [Run a Gradio Chat Interface](/v4/examples/asgi-gradio-interface)

Join our Community [Discord](https://discord.gg/ATj6USmeE2) for support and updates.
