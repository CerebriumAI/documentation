available-hardware.mdx

# Vendored byte-for-byte from skills/cerebrium/SKILL.md in CerebriumAI/cerebrium-skills.
# A checker there fails that repo's CI on any drift, and Prettier re-wraps this file
# if allowed to touch it. Edit the skill upstream, never here.
/skill.md

# Vendored from skills/cerebrium/references/config.md in CerebriumAI/cerebrium-skills so the
# relative references/config.md link inside skill.md resolves. Edit upstream, never here.
/references/config.md

# Prettier rewrites the JSX comment {/* ... */} in this file to {/_ ... _/}, which MDX
# cannot parse, and HTML comments <!-- --> are not valid MDX either. Keep Prettier off
# this file so the heading-anchor comment survives.
/performance/checkpointing.mdx
